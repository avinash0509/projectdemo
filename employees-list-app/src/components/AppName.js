import "./AppName.css";
const AppName = ({appTitle}) => {

  return (
    <div className="container">
      <h1 className="app-text"> {appTitle} List App</h1>
    </div>
  );
};
export default AppName;
